# Node.js (Express) app using PostgreSQL(Sequelize.js)


### Before Running

`cp env.sample .env`

## Usage

`docker-compose up`

### Database connection

`docker exec -it <postgres_container_id> psql -U postgres`
